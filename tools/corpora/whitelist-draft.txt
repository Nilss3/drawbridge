# DRAFT default whitelist -- app_ratings.allowed_packages
#
# Every entry here is a package the rating rule would otherwise REMOVE (Parental
# guidance, or PEGI 7+) that a household plausibly needs. Drafted 2026-08-17 from
# the two corpus runs in this directory; see docs/app-ratings.md.
#
# Two standing rules, both checkable:
#   1. Nothing here may appear in blocked_packages. The whitelist is evaluated
#      BEFORE the blocklist, so an overlap silently unblocks a blocked app.
#   2. Nothing here may be governed by an option. WhatsApp and Telegram are
#      deliberately absent: whitelisting them would override the parent's switch
#      and make the option a decoration.

# --- private messengers (all Parental guidance: ungraded conversation) --------
ch.threema.app
network.loki.messenger
chat.simplex.app

# --- conferencing ------------------------------------------------------------
us.zoom.videomeetings
com.cisco.wx2.android

# --- general-purpose AI assistants -------------------------------------------
# Not blockable honestly -- every allowed browser reaches one through its search
# box. See "Chatbots are not blocked" in docs/app-ratings.md. Companions are NOT
# here and stay blocked.
com.openai.chatgpt
com.anthropic.claude
com.google.android.apps.bard
ai.perplexity.app.android
com.deepseek.chat
ai.mistral.chat

# --- sport and fitness (community feeds make them ungraded) ------------------
com.strava
com.stt.android
com.fitapp

# --- recipes and food --------------------------------------------------------
com.mufumbo.android.recipe.search
com.recime.app
com.foodient.whisk

# --- nature and hobbies ------------------------------------------------------
com.chirpbirding.birda
com.apex.community.birdbrain.birdbrain

# --- health and pharmacy (PEGI 18 because they sell medicines) ---------------
# Regional. A household outside Belgium needs its own, which is what the
# device-local half of the whitelist is for.
app.newpharma.shop

# --- music and audiobooks (not video: that is the streaming option's business)
com.spotify.music
com.audible.application

# --- family photo sharing ----------------------------------------------------
us.mitene

# --- DELIBERATELY EXCLUDED, recorded so the absence reads as a decision -------
# com.whatsapp             -- has an option
# org.telegram.messenger   -- has an option
# com.netflix.mediaclient  -- streaming option
# com.facebook.orca        -- Messenger: social, not a private messenger
# com.tencent.mm           -- WeChat: same
# com.viber.voip           -- Viber: same
# com.imo.android.imoim    -- imo: same
# com.ebay.mobile          -- shopping; add locally if wanted
