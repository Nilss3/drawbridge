<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/toolbar_background">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/tabsToolbarTop"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/toolbar_background"
        app:title="@string/tabs_tray_title"
        app:layout_constraintTop_toTopOf="parent"
        app:titleTextColor="@color/toolbar_text" />

    <!-- The root is toolbar-coloured, so the strips it pads itself by under the
         system bars match the two bars. The tab list carries the theme
         background instead. -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/tabsTray"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:background="?android:attr/colorBackground"
        app:layout_constraintBottom_toTopOf="@+id/tabsActionBar"
        app:layout_constraintTop_toBottomOf="@+id/tabsToolbarTop" />

    <LinearLayout
        android:id="@+id/tabsActionBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/toolbar_background"
        android:orientation="horizontal"
        android:padding="8dp"
        app:layout_constraintBottom_toBottomOf="parent">

        <Button
            android:id="@+id/newTabButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tabs_tray_new_tab" />

        <Button
            android:id="@+id/closeAllTabsButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tabs_tray_close_all" />

        <Button
            android:id="@+id/doneButton"
            style="?attr/borderlessButtonStyle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/tabs_tray_done" />
    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
